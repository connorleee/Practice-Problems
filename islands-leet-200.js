var numIslands = function(grid) {
    const islands = new Set();
    
    let islandCount = 0;
    
    for(let y = 0; y < grid.length; y++) {
        for(let x = 0; x < grid[y].length) { 
            
            if(grid[y][x] === 1){
                let     
            }
        }
    }
    
    return islandCount;
};